{% assign type = "" %}
<div style="color:black;">
{{ template }}
</div>
{% if
      template contains "metaobject/men" or
      template contains "account/men" or
      page.metafields.page.type == "men" or 
      collection.template_suffix == "men" or
      product.template_suffix == "men" or
      template contains "search.men"
%}
  {% assign type = "men" %}
  
{% elsif
        template contains "metaobject/women" or
        template contains "account/women" or
        page.metafields.page.type == "women" or
        collection.template_suffix == "women" or
        product.template_suffix == "women" or
        template contains "search.women"
%}
  {% assign type = "women" %}

{% endif %}